buildTag=0.2pre

groovy.home=bin/groovy

classes=build/classes

httpcat=http-cat
httpcatBt=${httpcat}-${buildTag}
httpcatBtDate=${httpcatBt}-${DSTAMP}
httpcatJar=${httpcatBtDate}.jar
httpcatSh=bin/${httpcat}.sh
httpcatBat=bin/${httpcat}.bat

grepjars=grep-jars
grepjarsBt=${grepjars}-${buildTag}
grepjarsBtDate=${grepjarsBt}-${DSTAMP}
grepjarsJar=${grepjarsBtDate}.jar
grepjarsSh=${grepjars}.sh
grepjarsBat=${grepjars}.bat

groovy=groovy
groovyJar=${groovy}.jar
